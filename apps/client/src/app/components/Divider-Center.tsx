import { Divider } from '@client/components/Divider';

export const DividerCenter: React.FC = () => (
   <div className="flex w-full flex-col items-center justify-center">
      <Divider variant="horizontal" className="w-8/12" />
   </div>
);
